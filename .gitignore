node_modules
dist
.DS_Store
.env

# Rendered output. "out" is a symlink to ~/Desktop/motion-renders, so every
# render — CLI or the Studio's Render button — lands in that Desktop folder.
/out

# Local Claude Code permissions, per-machine.
.claude/settings.local.json

# remotion bundle output
/build
